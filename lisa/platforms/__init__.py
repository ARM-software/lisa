import json
import os
#  from lisa.conf import JsonConf

#TODO: remove that

# Add identifiers for each of the platform we have JSON descriptors for
#  this_dir = os.path.dirname(__file__)
#  for file_name in os.listdir(this_dir):
    #  name, ext = os.path.splitext(file_name)
    #  if ext == '.json':
        #  platform = JsonConf.from_path(os.path.join(this_dir, file_name))
        #  identifier = name.replace('-', '_')
        #  globals()[identifier] = platform

# vim :set tabstop=4 shiftwidth=4 textwidth=80 expandtab
